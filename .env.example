DATABASE_URL="postgresql://postgres:postgres@localhost:5432/clearpath?schema=public"
AUTH_SECRET="dev-only-secret-change-me-0123456789abcdef"
# Directory where student evidence images are stored on disk (relative to the project root).
UPLOAD_DIR="uploads"
